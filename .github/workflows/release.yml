name: Create Extension Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write

jobs:
  build:
    name: Package and Release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Create Release Zip
        run: |
          zip -r claimai-extension.zip . \
            -x "*.git*" \
            -x "*.github*" \
            -x "Romanstone/*" \
            -x "tests/*" \
            -x "*.gemini*" \
            -x "README.md" \
            -x "SECURITY.md" \
            -x "ClaimAi/.git*" \
            -x "ICD-10-CM/*" \
            -x "scripts/*" \
            -x "lib/icd10-index.js" \
            -x "rules/pmb-normalized.json" \
            -x "rules/*.csv" \
            -x "ClaimAi/data/*" \
            -x "ClaimAi/lib/*" \
            -x "ClaimAi/convert-icd.js"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: claimai-extension.zip
          draft: true
          prerelease: false
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
